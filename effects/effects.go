package effects

// "ledfx/devices"
// "ledfx/color"

func SampleEffect() { //device Device) {
	// TODO:
	// create a loop that sends data to a device for each frame

	// REMOVEME:
	// Test by sending one frame and seeing if it changes the LEDs

	// data := [1]{color.Color{255, 0, 0}}
	// devices.SendDeviceData(device, data)
}
